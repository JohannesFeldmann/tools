# Clone repository
git clone "repository" "directory"

# Aktuelle Version, Änderungen anzeigen
git log
git status
git diff
git diff (commitA commitB)

##### commits

# Commit 
git commit -m "message"

# Undo certain commit
git revert "name of commit"

# Letzten commit rückgängig machen
reset --hard HEAD

# Merge
git merge

##### branches

# Neue branch aus aktueller branch erstellen
git checkout -b "newbranch"

# Create and checkout local branch from remote repository
git checkout -b test origin/test

# Delete branch "foo"
git branch -d foo

##### remote repositories

# Add another remote repository "origin2"
git remote add origin2 git@github.com:foo/bar.git

# Check remote repositories
git remote -v

# Pull from certain repository "origin2"
git pull origin2






